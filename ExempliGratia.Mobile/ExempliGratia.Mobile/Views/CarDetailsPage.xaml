<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="ExempliGratia.Mobile.Views.CarDetailsPage"
             BackgroundColor="White"
             Title="Car details">
    
    <ContentPage.Content>
    
        <Grid Margin="16">
        
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            
            <Image Source="{Binding PhotoUrl}" 
                   Aspect="AspectFit"
                   VerticalOptions="Start"
                   Grid.Row="0"/>

            <Label Text="{Binding CarTitle}"
                   FontAttributes="Bold"
                   FontSize="Medium"
                   HorizontalOptions="Center"
                   Grid.Row="1" />
        </Grid>
    </ContentPage.Content>
</ContentPage>